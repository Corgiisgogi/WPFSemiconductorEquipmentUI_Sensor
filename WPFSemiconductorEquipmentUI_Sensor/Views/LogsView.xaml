<UserControl x:Class="WPFSemiconductorEquipmentUI_Sensor.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:WPFSemiconductorEquipmentUI_Sensor.Controls">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
        <DockPanel>
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="활동 / 센서 로그" Style="{StaticResource PageTitleText}" />
                <TextBlock Text="저장된 활동 로그와 센서 기록을 조회합니다." Style="{StaticResource BodyText}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <controls:StatusBadge Text="SQLite 저장" Tone="Normal" Margin="0,0,8,0" />
                <controls:StatusBadge Text="감사 로그" Tone="Blue" Margin="0,0,8,0" />
                <controls:StatusBadge Text="{Binding FilterStatusText}" Tone="{Binding FilterStatusTone}" />
            </StackPanel>
        </DockPanel>

        <Border Margin="0,16,0,0" Style="{StaticResource CardBorderStyle}" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="114" />
                    <ColumnDefinition Width="114" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="126" />
                    <ColumnDefinition Width="112" />
                    <ColumnDefinition Width="*" MinWidth="150" />
                    <ColumnDefinition Width="230" />
                </Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="시작일" Style="{StaticResource CaptionText}" />
                    <TextBox Text="{Binding FromDateText, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="종료일" Style="{StaticResource CaptionText}" />
                    <TextBox Text="{Binding ToDateText, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="사용자" Style="{StaticResource CaptionText}" />
                    <ComboBox ItemsSource="{Binding UserFilterOptions}" SelectedItem="{Binding SelectedUserFilter}" />
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="0,0,10,0">
                    <TextBlock Text="유형" Style="{StaticResource CaptionText}" />
                    <ComboBox ItemsSource="{Binding SourceFilterOptions}" SelectedItem="{Binding SelectedSourceFilter}" />
                </StackPanel>
                <StackPanel Grid.Column="4" Margin="0,0,10,0">
                    <TextBlock Text="등급" Style="{StaticResource CaptionText}" />
                    <ComboBox ItemsSource="{Binding SeverityFilterOptions}" SelectedItem="{Binding SelectedSeverityFilter}" />
                </StackPanel>
                <StackPanel Grid.Column="5" Margin="0,0,10,0">
                    <TextBlock Text="검색어" Style="{StaticResource CaptionText}" />
                    <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="검색" Command="{Binding SearchCommand}" Style="{StaticResource CompactButtonStyle}" Width="72" Margin="0,0,6,0" />
                    <Button Content="초기화" Command="{Binding ResetFilterCommand}" Style="{StaticResource CompactButtonStyle}" Width="72" Margin="0,0,6,0" />
                    <Button Content="내보내기" Command="{Binding ExportCommand}" Style="{StaticResource CompactButtonStyle}" Width="80" />
                </StackPanel>
            </Grid>
        </Border>

        <UniformGrid Columns="4" Margin="0,16,0,0">
            <Border Style="{StaticResource CardBorderStyle}" MinHeight="124" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <controls:StatusBadge Text="전체 로그" Tone="Blue" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding TotalLogCountText}" Margin="0,10,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                    <TextBlock Text="SQLite 저장 기준" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" MinHeight="124" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <controls:StatusBadge Text="경고" Tone="Warning" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding WarningCountText}" Margin="0,10,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                    <TextBlock Text="AI 감시 / 센서 임계값" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" MinHeight="124" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <controls:StatusBadge Text="위험 이벤트" Tone="Danger" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding RiskCountText}" Margin="0,10,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                    <TextBlock Text="정지 관련 이벤트" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" MinHeight="124" Padding="16">
                <StackPanel>
                    <controls:StatusBadge Text="최근 정지" Tone="Normal" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding LastShutdownText}" Margin="0,10,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                    <TextBlock Text="최근 정지 명령 시각" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Margin="0,18,0,0" MinHeight="472">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="600" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="276" />
            </Grid.ColumnDefinitions>
            <DataGrid ItemsSource="{Binding ActivityLogs}"
                      SelectedItem="{Binding SelectedLog}"
                      MaxHeight="472"
                      VerticalAlignment="Top"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionUnit="FullRow"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="False"
                      CanUserResizeRows="False"
                      CanUserSortColumns="False"
                      AlternationCount="2">
                <DataGrid.Resources>
                    <Style x:Key="LogCellTextStyle" TargetType="TextBlock">
                        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                </DataGrid.Resources>
                <!-- 등급별 행 배경: INFO는 기본(교차 배경) 유지, WARN=연노랑, RISK=연빨강 -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Severity}" Value="WARN">
                                <Setter Property="Background" Value="{StaticResource BrushWarningSoft}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Severity}" Value="RISK">
                                <Setter Property="Background" Value="{StaticResource BrushDangerSoft}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="날짜" Binding="{Binding DateText}" Width="92" ElementStyle="{StaticResource LogCellTextStyle}" />
                    <DataGridTextColumn Header="시간" Binding="{Binding Time}" Width="72" ElementStyle="{StaticResource LogCellTextStyle}" />
                    <DataGridTextColumn Header="출처" Binding="{Binding Source}" Width="76" ElementStyle="{StaticResource LogCellTextStyle}" />
                    <DataGridTextColumn Header="사용자" Binding="{Binding User}" Width="78" ElementStyle="{StaticResource LogCellTextStyle}" />
                    <DataGridTextColumn Header="이벤트" Binding="{Binding Event}" Width="*" ElementStyle="{StaticResource LogCellTextStyle}" />
                    <DataGridTextColumn Header="등급" Binding="{Binding Severity}" Width="72" ElementStyle="{StaticResource LogCellTextStyle}" />
                    <DataGridTextColumn Header="저장" Binding="{Binding Saved}" Width="58" ElementStyle="{StaticResource LogCellTextStyle}" />
                </DataGrid.Columns>
            </DataGrid>
            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="선택 로그 상세" Style="{StaticResource SectionTitleText}" />
                    <controls:StatusBadge Text="{Binding SelectedLogDetailIdText}" Tone="Blue" HorizontalAlignment="Left" Margin="0,14,0,0" />
                    <TextBlock Text="{Binding SelectedLogEventText}" Margin="0,14,0,0" Style="{StaticResource BodyText}" TextWrapping="Wrap" />
                    <Border Margin="0,16,0,0" Padding="12" CornerRadius="8" Background="{StaticResource BrushSurfaceSubtle}">
                        <StackPanel>
                            <TextBlock Text="센서 스냅샷" Style="{StaticResource BodyStrongText}" />
                            <TextBlock Text="{Binding SensorSnapshotTimeText}" Margin="0,8,0,0" Style="{StaticResource CaptionText}" />
                            <DockPanel Margin="0,12,0,0"><TextBlock Text="압력" Style="{StaticResource CaptionText}" /><controls:StatusBadge DockPanel.Dock="Right" Text="{Binding DetailPressureText}" Tone="Blue" /></DockPanel>
                            <DockPanel Margin="0,10,0,0"><TextBlock Text="진동" Style="{StaticResource CaptionText}" /><controls:StatusBadge DockPanel.Dock="Right" Text="{Binding DetailVibrationText}" Tone="Blue" /></DockPanel>
                            <DockPanel Margin="0,10,0,0"><TextBlock Text="온도" Style="{StaticResource CaptionText}" /><controls:StatusBadge DockPanel.Dock="Right" Text="{Binding DetailTemperatureText}" Tone="Blue" /></DockPanel>
                            <DockPanel Margin="0,10,0,0"><TextBlock Text="습도" Style="{StaticResource CaptionText}" /><controls:StatusBadge DockPanel.Dock="Right" Text="{Binding DetailHumidityText}" Tone="Blue" /></DockPanel>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="{Binding SelectedLogDateTimeText}" Margin="0,14,0,0" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding SelectedLogSourceText}" Margin="0,8,0,0" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding SelectedLogActorText}" Margin="0,8,0,0" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="{Binding SelectedLogSeverityText}" Margin="0,8,0,0" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="{Binding SelectedLogSavedText}" Margin="0,8,0,0" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
