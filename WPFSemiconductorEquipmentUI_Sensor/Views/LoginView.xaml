<UserControl x:Class="WPFSemiconductorEquipmentUI_Sensor.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:WPFSemiconductorEquipmentUI_Sensor.Controls">
    <StackPanel>
        <DockPanel>
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleText}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <controls:StatusBadge Text="DB 연결됨" Tone="Normal" Margin="0,0,8,0" />
                <controls:StatusBadge Text="{Binding AuthStatusText}" Tone="{Binding AuthStatusTone}" Width="104" HorizontalAlignment="Right" />
            </StackPanel>
        </DockPanel>

        <Grid Margin="0,18,0,0" MinHeight="640">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="500" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardBorderStyle}" Padding="28">
                <StackPanel>
                    <controls:StatusBadge Text="제어 시스템" Tone="Blue" HorizontalAlignment="Left" />
                    <TextBlock Text="반도체 장비 제어 시스템"
                               Margin="0,18,0,0"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{StaticResource BrushTextPrimary}"
                               TextWrapping="Wrap" />

                    <Border Margin="0,24,0,0" Padding="16" CornerRadius="8" Background="{StaticResource BrushSurfaceSubtle}">
                        <StackPanel>
                            <TextBlock Text="현재 로그인 정보" Style="{StaticResource BodyStrongText}" />
                            <DockPanel Margin="0,14,0,0">
                                <TextBlock Text="승인 상태" Style="{StaticResource CaptionText}" />
                                <controls:StatusBadge DockPanel.Dock="Right" Text="{Binding Session.UserStateText}" Tone="{Binding Session.UserStateTone}" Width="104" />
                            </DockPanel>
                            <DockPanel Margin="0,10,0,0">
                                <TextBlock Text="권한" Style="{StaticResource CaptionText}" />
                                <controls:StatusBadge DockPanel.Dock="Right" Text="{Binding Session.RoleText}" Tone="Blue" Width="104" />
                            </DockPanel>
                            <TextBlock Text="{Binding Session.UserId, StringFormat=사용자 ID: {0}}" Margin="0,12,0,0" Style="{StaticResource CaptionText}" />
                        </StackPanel>
                    </Border>

                    <Border Margin="0,18,0,0" Padding="16" CornerRadius="8" Background="{StaticResource BrushSurfaceSubtle}" Visibility="{Binding AuthEntryVisibility}">
                        <StackPanel>
                            <TextBlock Text="접근 절차" Style="{StaticResource BodyStrongText}" />
                            <TextBlock Text="01  회원가입 요청" Margin="0,12,0,0" Style="{StaticResource CaptionText}" />
                            <TextBlock Text="02  웹 관리자 승인" Margin="0,8,0,0" Style="{StaticResource CaptionText}" />
                            <TextBlock Text="03  승인 계정 로그인" Margin="0,8,0,0" Style="{StaticResource CaptionText}" />
                        </StackPanel>
                    </Border>

                    <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                        <controls:StatusBadge Text="EtherCAT 준비" Tone="Normal" Margin="0,0,8,0" />
                        <controls:StatusBadge Text="SQLite 저장" Tone="Normal" Margin="0,0,8,0" />
                        <controls:StatusBadge Text="AI 감시" Tone="Warning" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Padding="28">
                <StackPanel>
                    <DockPanel>
                        <StackPanel>
                            <TextBlock Text="사용자 인증" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                        </StackPanel>
                        <controls:StatusBadge DockPanel.Dock="Right" Text="{Binding AuthStatusText}" Tone="{Binding AuthStatusTone}" Width="104" HorizontalAlignment="Right" />
                    </DockPanel>

                    <StackPanel Visibility="{Binding LoggedInPanelVisibility}" Margin="0,22,0,0">
                        <Border Padding="16" CornerRadius="8" Background="{StaticResource BrushNormalSoft}" BorderBrush="#A7D9C4" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="로그인 상태" Foreground="{StaticResource BrushNormal}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Session.UserId, StringFormat=사용자 ID: {0}}" Margin="0,12,0,0" Style="{StaticResource BodyStrongText}" />
                                <TextBlock Text="{Binding Session.RoleText, StringFormat=권한: {0}}" Margin="0,8,0,0" Style="{StaticResource BodyText}" />
                                <TextBlock Text="{Binding Session.UserStateText, StringFormat=상태: {0}}" Margin="0,6,0,0" Style="{StaticResource BodyText}" />
                            </StackPanel>
                        </Border>
                        <Button Content="로그아웃" Command="{Binding LogoutCommand}" Style="{StaticResource CompactButtonStyle}" Width="132" Margin="0,18,0,0" HorizontalAlignment="Left" />
                    </StackPanel>

                    <StackPanel Visibility="{Binding AuthEntryVisibility}" Margin="0,18,0,0">
                        <Border Padding="6" CornerRadius="8" Background="{StaticResource BrushSurfaceSubtle}">
                            <UniformGrid Columns="2">
                                <Button Content="로그인"
                                        Command="{Binding ShowLoginCommand}"
                                        Style="{StaticResource CompactButtonStyle}"
                                        Margin="0,0,6,0" />
                                <Button Content="회원가입"
                                        Command="{Binding ShowSignUpCommand}"
                                        Style="{StaticResource CompactButtonStyle}"
                                        Margin="6,0,0,0" />
                            </UniformGrid>
                        </Border>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <controls:StatusBadge Text="로그인 모드" Tone="{Binding LoginModeTone}" Margin="0,0,8,0" />
                            <controls:StatusBadge Text="회원가입 모드" Tone="{Binding SignUpModeTone}" />
                        </StackPanel>

                        <StackPanel Margin="0,18,0,0" Visibility="{Binding LoginPanelVisibility}">
                            <TextBlock Text="작업자 로그인" Style="{StaticResource SectionTitleText}" />
                            <TextBlock Text="사용자 ID" Margin="0,18,0,6" Style="{StaticResource CaptionText}" />
                            <TextBox Text="{Binding LoginUserId, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="비밀번호" Margin="0,14,0,6" Style="{StaticResource CaptionText}" />
                            <PasswordBox x:Name="LoginPasswordInput" PasswordChanged="OnLoginPasswordChanged" Height="40" Padding="12,0" VerticalContentAlignment="Center" />
                            <WrapPanel Margin="0,18,0,0">
                                <Button Content="로그인" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="132" Margin="0,0,12,10" />
                                <Button Content="상태 확인" Command="{Binding CheckStatusCommand}" Style="{StaticResource CompactButtonStyle}" Width="132" Margin="0,0,0,10" />
                            </WrapPanel>
                        </StackPanel>

                        <StackPanel Margin="0,18,0,0" Visibility="{Binding SignUpPanelVisibility}">
                            <TextBlock Text="회원가입 요청" Style="{StaticResource SectionTitleText}" />
                            <TextBlock Text="사용자 ID" Margin="0,18,0,6" Style="{StaticResource CaptionText}" />
                            <TextBox Text="{Binding RegisterUserId, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="표시 이름" Margin="0,14,0,6" Style="{StaticResource CaptionText}" />
                            <TextBox Text="{Binding RegisterDisplayName, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="비밀번호" Margin="0,14,0,6" Style="{StaticResource CaptionText}" />
                            <PasswordBox x:Name="RegisterPasswordInput" PasswordChanged="OnRegisterPasswordChanged" Height="40" Padding="12,0" VerticalContentAlignment="Center" />
                            <TextBlock Text="비밀번호 확인" Margin="0,14,0,6" Style="{StaticResource CaptionText}" />
                            <PasswordBox x:Name="RegisterConfirmPasswordInput" PasswordChanged="OnRegisterConfirmPasswordChanged" Height="40" Padding="12,0" VerticalContentAlignment="Center" />
                            <WrapPanel Margin="0,18,0,0">
                                <Button Content="가입 요청" Command="{Binding RegisterCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="132" Margin="0,0,12,10" />
                                <Button Content="상태 확인" Command="{Binding CheckStatusCommand}" Style="{StaticResource CompactButtonStyle}" Width="132" Margin="0,0,12,10" />
                                <Button Content="로그인으로" Command="{Binding ShowLoginCommand}" Style="{StaticResource CompactButtonStyle}" Width="132" Margin="0,0,0,10" />
                            </WrapPanel>
                        </StackPanel>
                    </StackPanel>

                    <Border Margin="0,18,0,0" Padding="16" CornerRadius="8" Background="{StaticResource BrushWarningSoft}" BorderBrush="#E7C26A" BorderThickness="1">
                        <StackPanel>
                            <DockPanel>
                                <TextBlock Text="인증 결과" Foreground="{StaticResource BrushWarning}" FontWeight="SemiBold" />
                                <controls:StatusBadge DockPanel.Dock="Right" Text="{Binding AuthStatusText}" Tone="{Binding AuthStatusTone}" Width="104" HorizontalAlignment="Right" />
                            </DockPanel>
                            <TextBlock Text="{Binding AuthMessage}"
                                       Style="{StaticResource BodyText}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
